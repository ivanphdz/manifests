{
    "LABEL": {
      "name": "Invalid PR Title",
      "color": "B60205"
    },
    "CHECKS": {
      "regexp": "/^(perf|feat|fix)\\[([A-Z]{2,}-\\d+|no ticket)\\]:?.*/i",
      "ignoreLabels" : ["ignore-title"]
    }
}
